<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 使用 <uses-feature> 宣告相機要求 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <!-- 1. 核心與媒體權限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/> <!-- 建議將 maxSdkVersion 設為 32 (Android 12L) 或更低，因為 Android 13+ 應使用 READ_MEDIA_* -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 2. 通知與前景服務權限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- 🌟【關鍵修復】新增 mediaPlayback 服務類型權限 (API 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />


    <application
        android:name=".PlantPokedexApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeerAdviser"
        tools:targetApi="35">

        <!-- Activity 宣告  -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityB" />
        <activity android:name=".InputActivity" />
        <activity android:name=".LevelActivity" />
        <activity android:name=".PokedexActivity" />
        <activity android:name=".NoteDetailActivity" />

        <!-- 為服務指定前景服務類型  -->
        <service
            android:name=".BackgroundMusicService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- FileProvider 宣告 (保持不變) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>